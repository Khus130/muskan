#include <stdio.h>
int main()
{
   int N,count;
   scanf("%d",&N);
   while(N>0)
   {
       for(int i=1; i<N; i++)
       {
           if(N%i==0);
       }
       printf("%d",i);
   }
   printf("\n");
}
